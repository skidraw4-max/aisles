node_modules
# Keep environment variables out of version control
.env
.env*.local

.next

/generated/prisma

# 빌드 시 ADS_TXT 로 생성 (저장소에 pub ID 커밋 방지)
public/ads.txt

# Capacitor mobile shell (android/ is committed for reproducibility)
mobile/node_modules/
mobile/.gradle/
mobile/local.properties
mobile/*.jks
mobile/*.keystore
mobile/key.properties
mobile/android/*.jks
mobile/android/*.keystore
mobile/android/keystore.properties
